package pl.thinkdata.droptop.database.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ImportRaportRepository extends JpaRepository<ImportRaportRepository, Long> {
}
